services:
  - type: web
    name: reference-podcast-discord-bot
    runtime: python
    plan: free
    buildCommand: python -m pip install --upgrade pip && python -m pip install .
    startCommand: python start.py
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DATABASE_PATH
        value: episodes.sqlite3
      - key: DISCORD_TOKEN
        sync: false
      - key: DISCORD_GUILD_IDS
        sync: false
